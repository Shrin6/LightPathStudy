ROLE: You are Replit’s AI coding assistant working on an existing web app called “LightPath Study”.

GOAL
Give the app a “strong, compact, modern” UI makeover (like a polished SaaS dashboard) WITHOUT changing any core functionality, logic, routes, database schema, edge functions, or API behavior.

ABSOLUTE RULES (DO NOT BREAK)
1) NO backend changes:
   - Do NOT touch Supabase schema, RLS, SQL, RPC functions (ex: match_document_chunks), edge functions (parse-document, chat-tutor), or storage.
2) NO business logic changes:
   - Do NOT alter parsing, chunking, embeddings, quiz generation logic, scoring logic, or any request/response payloads.
   - Do NOT change how data is saved, fetched, or validated.
3) NO feature removals:
   - Keep every panel/flow working exactly the same (Upload, Tutor, Quiz, Flashcards, Notes, Worksheet, Reports, etc.).
4) UI-only changes allowed:
   - You MAY change layout, spacing, typography, colors, icons, component styling, responsiveness, and visual hierarchy.
   - You MAY refactor repeated UI styling into shared components (purely presentational).
   - You MAY rename CSS classes and reorganize UI components as long as behavior stays identical.
5) Keep it readable and accessible:
   - Ensure contrast, clear focus states, keyboard navigation, and good spacing.
6) Avoid “weird characters/stars”:
   - Remove decorative symbols like **, random bullets from the UI (but don’t modify the underlying content text stored in DB).
   - Only clean formatting at render-time (presentation layer).

DESIRED LOOK & FEEL
- Compact, confident dashboard layout
- Modern sidebar with clear navigation
- Strong header with search + user menu
- Clean cards, subtle borders, soft shadows, consistent spacing
- Better typography scale (title, subtitle, body, muted)
- Consistent button styles (primary/secondary/ghost)
- Consistent panel layout for Tutor/Quiz/Flashcards/etc.
- Mobile responsive (sidebar collapses)
- No clutter

SCOPE: WHAT TO CHANGE (UI TARGETS)
A) Global Layout
- Create a base “AppShell” layout:
  - Left Sidebar (collections, modes)
  - Top Header (page title + search + actions)
  - Main Content Area (mode panels)
- Make everything look intentional: alignments, spacing, consistent widths.

B) Sidebar
- Compact sections:
  - Brand: “LightPath Study”
  - Primary nav: Explain, Quiz, Flashcards, Worksheet, Notes, Reports
  - Collections list: scrollable, with active state highlight
  - Upload button: prominent but not huge
- Add collapse behavior for smaller screens.

C) Panels (Tutor / Quiz / Flashcards / Notes / Worksheet)
- Standardize panel container:
  - Title row (left: title + subtitle, right: actions)
  - Content area with consistent padding
- For Tutor chat:
  - Make chat bubbles cleaner
  - Make input sticky at bottom
  - Add subtle separators
  - Keep message rendering logic identical

D) Quiz UI improvements (NO logic change)
- Keep the same quiz data flow.
- Improve readability:
  - Better spacing between question and options
  - Clear “selected”, “correct”, “incorrect” states
  - Add an explanation area under the answer AFTER user checks (if explanation exists)
  - If explanation is not returned, show nothing (don’t invent explanations)
- Remove the “>100%” display issue visually if it occurs:
  - Clamp displayed percentage between 0–100 in UI only (do not change stored score logic).
  - Example: displayPercent = Math.min(100, Math.max(0, computedPercent))

E) Upload area
- Make upload area look like a modern dropzone.
- Show a clean file list with status badges (Processing / Ready).
- Do not change upload mechanics.

TECH CONSTRAINTS
- Keep existing component library usage (shadcn/ui already present).
- Use Tailwind for styling.
- Prefer editing existing components rather than rewriting everything.
- Do not add new dependencies unless absolutely required for UI only.
  - If you must add one, justify it and keep it minimal. Otherwise avoid.

WORKFLOW REQUIREMENTS
1) First: Audit UI entry points
   - Identify the main page layout components (likely Study page, panels).
   - Identify shared UI components.
2) Then: Implement AppShell
   - Introduce a layout wrapper without changing routing.
3) Then: Style passes
   - Sidebar, header, then each panel.
4) Final: Verify nothing broke
   - Upload still works
   - Tutor still responds
   - Quiz still generates
   - No changes to API payloads
   - No changes to DB writes
   - No TypeScript errors

DELIVERABLE
- Provide a clean, modern dashboard UI like a production SaaS.
- Keep every existing feature working exactly as before.

START NOW
Step 1: Find the Study page and the components used for:
- Sidebar/Collections
- ChatPane / Tutor
- QuizPanel
- FlashcardsViewer
- NotesViewer
- WorksheetPanel
Implement the UI-only refactor with AppShell and consistent styling.
