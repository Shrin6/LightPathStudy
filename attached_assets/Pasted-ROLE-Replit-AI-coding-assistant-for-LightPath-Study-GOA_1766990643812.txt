ROLE: Replit AI coding assistant for “LightPath Study”.

GOAL
Apply the SAME “strong, compact, modern SaaS” UI makeover to:
1) Dashboard page
2) Home/Landing page
…WITHOUT changing any core logic, routing behavior, auth rules, DB, edge functions, or API payloads.

ABSOLUTE RULES (DO NOT BREAK)
- UI ONLY. No backend/schema/RPC/edge function changes.
- No changes to how data is fetched/saved.
- No feature removals.
- No new dependencies unless UI-only + absolutely necessary (prefer none).
- Keep accessibility: focus states, keyboard nav, contrast.

DESIGN SYSTEM (ONE SOURCE OF TRUTH)
Implement a consistent design system across ALL pages:
- AppShell layout for authenticated app routes (Dashboard + Study)
- PublicShell layout for unauthenticated routes (Home)
- Consistent typography scale, spacing, buttons, inputs, cards, badges, tables
- Remove “weird characters/stars” from UI rendering only (don’t modify stored content)

GLOBAL LAYOUTS TO ADD
A) PublicShell (Home page)
- Top nav: logo “LightPath Study”, links (Features, How it works, Pricing/Alpha, Sign in)
- Hero section: bold headline, subhead, primary CTA (“Try Alpha”), secondary CTA (“See Demo”)
- Feature grid: 3–6 feature cards
- “How it works” steps: Upload → Learn → Quiz/Flashcards → Improve
- Social proof/FAQ section (simple)
- Footer: minimal links

B) AppShell (Dashboard + Study)
- Left sidebar:
  - Brand
  - Primary nav: Dashboard, Study, Reports, Settings (whatever exists—don’t add new routes)
  - Compact collection shortcuts if available (optional UI only)
- Top header:
  - Page title
  - Search input (UI only unless search already exists; if not, keep it non-functional or hidden)
  - User menu (sign out button if exists)
- Main content area:
  - Consistent container widths + padding

DASHBOARD PAGE (UI ONLY)
Objective: Make it look like a real product dashboard.

Dashboard content layout:
1) Top summary cards (3–4):
   - “Collections”
   - “Files Uploaded”
   - “Study Sessions”
   - “Accuracy / Quiz Score Avg” (ONLY if data already exists; otherwise show placeholders that don’t require new queries)
2) Recent activity card:
   - Recent uploads / sessions (use existing data calls only)
3) Quick actions:
   - “Upload file”
   - “Start Quiz”
   - “Open Flashcards”
   (These must use existing navigation/actions—no new API logic)

If dashboard currently has minimal content:
- Improve visual hierarchy and spacing
- Keep the same data usage
- Do not add new data dependencies

HOME/LANDING PAGE (UI ONLY)
- Modern hero with strong headline + 1-sentence promise
- Screenshot mock area (static image or existing assets only)
- Feature cards:
  - “Understand diagrams”
  - “Practice tests → quizzes”
  - “Flashcards that don’t break”
  - “Works with mixed files in one collection”
  - “Explain step-by-step”
- CTA section at bottom (Sign in / Start Alpha)
- Ensure mobile responsive

IMPLEMENTATION PLAN
1) Identify current routes/pages:
   - Find Home page component
   - Find Dashboard page component
   - Confirm Study page component
2) Create reusable layout wrappers:
   - components/layout/PublicShell.tsx
   - components/layout/AppShell.tsx
   - components/layout/AppHeader.tsx
   - components/layout/AppSidebar.tsx
3) Apply wrappers:
   - Home uses PublicShell
   - Dashboard + Study use AppShell
4) Style pass:
   - Buttons, cards, inputs, badges, tables
   - Consistent spacing and typography
5) Verify:
   - Auth flows unchanged
   - No changes to fetch calls
   - No new DB queries
   - No broken navigation

OUTPUT REQUIREMENT
- Make the UI look “compact, strong, premium” across Home + Dashboard + Study.
- Keep everything functional exactly as before.

START NOW
Locate the Home and Dashboard page files and implement PublicShell + AppShell.
Then refactor their markup to match the design system (UI only).
