I need my Page Builder / Preview Generator to stop using the same layout and become fully customizable by industry.

GOAL:
Generate a full-width, realistic homepage preview that:
- Looks different per industry
- Allows changing background, colors, fonts, buttons
- Uses scraped website info by default
- Allows manual override if scrape is weak
- Never shows broken images
- Feels like a real website “Home” page

--------------------------------
CORE BEHAVIOR
--------------------------------
1) When a website URL is provided:
- Scrape the website (prefer Firecrawl if available).
- Extract business info (name, phone, services, about, hours, location).
- Use that info to generate a preview homepage.

2) If scraped data is missing or weak:
- Allow Manual Mode to fill in missing info.
- Manual inputs always override scraped data.

--------------------------------
INDUSTRY PRESETS (IMPORTANT)
--------------------------------
Add a dropdown called “Industry Preset” with options:
- Restaurant / Food
- Plumbing / Home Services
- Lawyers / Law Firm
- Health / Clinic

Each preset MUST:
- Apply a different theme (colors, background, fonts, button style).
- Use a different homepage layout and section order.
- Change the overall look and feel noticeably.

--------------------------------
THEME SETTINGS (CUSTOMIZABLE)
--------------------------------
Each preview should be controlled by theme tokens:

{
  "background_style": "light | dark | gradient | image",
  "primary_color": "",
  "secondary_color": "",
  "font_style": "modern | classic | elegant | playful",
  "button_style": "solid | outline | pill",
  "card_style": "flat | shadow | border",
  "radius": "md | lg | xl",
  "spacing": "normal | roomy"
}

Add a “Customize” panel where I can change these and instantly update the preview.

--------------------------------
LAYOUT + SECTIONS (NO MORE SAME LAYOUT)
--------------------------------
Return a structured homepage layout with section variants.

Example structure:
{
  "layout_variant": "",
  "home_sections": [
    { "type": "hero", "variant": "", "headline": "", "subheadline": "", "ctaPrimary": "" },
    { "type": "services_or_menu", "variant": "", "items": [] },
    { "type": "trust_strip" },
    { "type": "gallery" },
    { "type": "about" },
    { "type": "contact" }
  ]
}

Different industries must use different:
- layout_variant
- section order
- section variants

--------------------------------
INDUSTRY RULES
--------------------------------
RESTAURANT:
- Emphasize menu, images, hours, location.
- Layout options: menu-first or gallery-first.

PLUMBING / HOME SERVICES:
- Emphasize trust, service areas, call now CTA.
- Layout options: service-first, call-first hero.

LAWYERS:
- Emphasize credibility, practice areas, consultation CTA.
- Use darker or premium styling.

HEALTH / CLINIC:
- Emphasize calm trust, services, providers, booking/call CTA.
- Clean, light design.

--------------------------------
IMAGES (CRITICAL)
--------------------------------
- Always prefer uploaded images (logo, hero, gallery).
- If an image is missing, show a clean placeholder with “Upload Image”.
- Never show broken image icons.

--------------------------------
OUTPUT REQUIREMENTS
--------------------------------
- Preview must be full-width (no boxed layout).
- Preview must look client-ready even with placeholders.
- Do not clone the original website.
- Do not invent awards, ratings, or certifications.

--------------------------------
SAVE TO LEAD
--------------------------------
Store on the lead:
- industry_preset
- theme_json
- layout_variant
- home_sections_json

--------------------------------
FINAL RESULT
--------------------------------
I should be able to:
- Change industry → preview looks completely different
- Change background/colors/fonts → preview updates
- Switch to manual mode if scrape looks bad
- Use this preview to send to customers
