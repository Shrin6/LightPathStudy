You are working on my app called **LightPath Study** (React + Supabase).

==============================
CORE RULES (NON-NEGOTIABLE)
==============================
- DO NOT modify or refactor:
  - Supabase schema, tables, RLS, or SQL
  - Edge functions (parse-document, chat-tutor, embeddings, OCR, etc.)
  - API calls, data flow, parsing, chunking, embeddings, or quiz logic
- DO NOT rename routes or change routing behavior
- DO NOT remove any existing features
- UI/UX changes ONLY
- All functionality must remain intact

Your job is strictly **visual + navigation improvements**.

==============================
PRIMARY GOAL
==============================
Perform a **UI makeover + navigation (backlinks)** to make the app feel:
- Strong, academic, and professional
- Compact and intentional (not “toy-like”)
- Easy to navigate for students using large collections
- Clear where the user is at all times

The app must still behave exactly the same.

==============================
PART 1: GLOBAL UI MAKEOVER
==============================

Apply a consistent design across **Home, Dashboard, Study pages**:

Design direction:
- Clean academic style (no stars, emojis, gimmicks, or decorative noise)
- Strong typography hierarchy
- Compact spacing
- Calm but confident color palette
- Modern card-based layout
- Professional, exam-focused look

Rules:
- Use existing UI system (shadcn/ui components)
- Improve spacing, alignment, font sizing, contrast
- Improve visual grouping of panels
- Make it feel like a serious study tool, not a generic website
- Mobile responsive, no layout breaking

DO NOT:
- Add new animations
- Add marketing fluff
- Add new features

==============================
PART 2: UI BACKLINKS / NAVIGATION
==============================

Add **context-aware backlinks** without touching logic.

### 1) GLOBAL BREADCRUMBS (Top of main content)
Show breadcrumb navigation at the top of content:

Examples:
- Home > Dashboard
- Home > Dashboard > Collection Name
- Home > Dashboard > Collection Name > Mode (Tutor / Quiz / Flashcards / Notes / Worksheet)

Rules:
- Breadcrumb items are clickable except the last
- If data not loaded yet, show “Loading…”
- On mobile, collapse breadcrumbs gracefully (Home > … > Mode)
- Styling: subtle, small text, muted color

### 2) BACK BUTTONS (Context-aware)
Add a left-aligned “← Back” button on:
- Study modes (Tutor, Quiz, Flashcards, Notes, Worksheet, Memory)
- Quiz results screen
- Reports / Settings (if present)

Behavior:
- Inside collection → “Back to Dashboard”
- Inside a mode → “Back to Collection”
- After quiz completion → “Back to Quiz” or “Back to Collection”

Implementation:
- Prefer React Router navigation
- If history stack is empty, fallback to correct route

### 3) DASHBOARD + HOME FOOTER NAV
Add a minimal footer (or bottom nav on mobile) with:
- Home
- Dashboard
- Study (if separate route)
- Settings / Reports (only if route exists)

Rules:
- Show active state
- Minimal styling
- No new routes

### 4) COLLECTION HEADER BAR
When a collection is selected, add a compact header with:
- Collection name
- “Back to Dashboard” link
- “Manage uploads” button (must use existing upload logic/dialog)

DO NOT create new backend logic.

==============================
PART 3: STUDY MODE UX POLISH
==============================

Improve clarity in study panels:
- Clear panel titles
- Strong visual separation between:
  - Tutor
  - Quiz
  - Flashcards
  - Notes
  - Worksheet
- Make it obvious what mode the user is in
- Reduce visual clutter
- Maintain all current behavior

==============================
DELIVERABLES
==============================
- List every file you changed
- For each file, explain what UI/navigation was added
- Confirm no Supabase, edge functions, or parsing logic was touched
- Describe how the UI should look after changes

==============================
START HERE
==============================
1) Identify the main layout / app shell
2) Create reusable components:
   - Breadcrumbs
   - BackButton
   - CollectionHeader
3) Integrate them into Home, Dashboard, Study, and mode pages
4) Apply consistent styling across pages

IMPORTANT:
This is a **UI-only refactor**.  
If something touches data, parsing, embeddings, or backend logic — STOP.
